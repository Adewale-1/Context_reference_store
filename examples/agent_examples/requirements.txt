# Requirements for agent examples and adapter demos
# These examples demonstrate Context Reference Store integration with various frameworks

# Core Context Reference Store (install from this directory)
# context-reference-store[all]

# Framework integrations (optional)
langchain>=0.1.0
langchain-core>=0.1.0
langgraph>=0.1.0
llama-index>=0.9.0
composio-core>=0.1.0

# Core ADK package for agent examples (install from the main adk-python repository)
# google-adk

# Environment variable management
python-dotenv>=1.0.0

# Performance monitoring
psutil>=5.8.0

# Note: To install the Context Reference Store package, run:
# pip install -e ../../
# 
# For ADK agent examples, also install ADK package from adk-python directory:
# pip install -e /path/to/adk-python
